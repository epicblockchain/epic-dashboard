name: Pull Request Opened

on:
    pull_request_target:
        types:
            - opened

jobs:
    pull-request-opened:
        uses: EPIC-BLOCKCHAIN/.github-private/.github/workflows/pull-request-opened-reusable.yml@main
        secrets:
            GH_EPICBLOCKCHAIN_USER_PAT_WRITE: ${{ secrets.GH_EPICBLOCKCHAIN_USER_PAT_WRITE }}
